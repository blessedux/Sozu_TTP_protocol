{
  "name": "@stellartap/protocol-qr",
  "version": "0.1.0",
  "description": "QR payload encoding/decoding for StellarTap payment requests (SEP-7 + request envelopes).",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "license": "MIT",
  "dependencies": {
    "@stellartap/protocol-core": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}

