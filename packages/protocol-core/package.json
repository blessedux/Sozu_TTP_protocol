{
  "name": "@stellartap/protocol-core",
  "version": "0.1.0",
  "description": "Core data models and encoding/decoding for StellarTap request envelopes.",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}

