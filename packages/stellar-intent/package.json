{
  "name": "@stellartap/stellar-intent",
  "version": "0.1.0",
  "description": "Transform StellarTap request envelopes into canonical Stellar transaction blueprints.",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "license": "MIT",
  "dependencies": {
    "@stellartap/protocol-core": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}

